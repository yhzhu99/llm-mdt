# Frontend

Browser-only React + Vite app for `llm-mdt`.

## Commands

```bash
npm install
npm run dev
npm run build
npm run test
npm run lint
```

## Notes

- Provider settings are configured in the UI, not via `.env`
- Conversations and settings are stored locally in the browser
- Deployment target is any static host such as Cloudflare Pages
